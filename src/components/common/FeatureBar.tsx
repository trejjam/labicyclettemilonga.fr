import {ReactNode} from "react";

export default function FeatureBar(
    {
        title,
        hide,
        action,
    }: {
        title: string,
        hide: boolean,
        action: ReactNode,
    }
) {
    return (
        <></>
    );
}
